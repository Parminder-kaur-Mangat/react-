import React from 'react';
import ReactDOM from 'react-dom';

const myName = (name) =>{
  return name.firstName + ' ' +name.lastName;
}

const obj = {
  firstName: 'Parminder', 
  lastName: 'Mangat' 
}

ReactDOM.render(
  <h1> Hello {myName(obj)}</h1>, 
  document.getElementById('root')
);
